import React from 'react'
import * as NBAIcons from 'react-nba-logos'

const CheckTeam = () => {
    if (teams.data[`${rando}`].abbreviation === TOR) {
         return <NBAIcons.TOR />
    } else if (teams.data[`${rando}`].abbreviation === LAL) {
        return <NBAIcons.LAL />
    } else if (teams.data[`${rando}`].abbreviation === ATL) {
        return <NBAIcons.ATL />
    } else if (teams.data[`${rando}`].abbreviation === BOS) {
        return <NBAIcons.BOS />
    } else if (teams.data[`${rando}`].abbreviation === BKN) {
        return <NBAIcons.BKN />
    } else if (teams.data[`${rando}`].abbreviation === CHA) {
        return <NBAIcons.CHA />
    } else if (teams.data[`${rando}`].abbreviation === CHI) {
        return <NBAIcons.CHI />
    } else if (teams.data[`${rando}`].abbreviation === CLE) {
        return <NBAIcons.CLE />
    } else if (teams.data[`${rando}`].abbreviation === DAL) {
        return <NBAIcons.DAL />
    } else if (teams.data[`${rando}`].abbreviation === DEN) {
        return <NBAIcons.DEN />
    } else if (teams.data[`${rando}`].abbreviation === DET) {
        return <NBAIcons.DET />
    } else if (teams.data[`${rando}`].abbreviation === GSW) {
        return <NBAIcons.GSW />
    } else if (teams.data[`${rando}`].abbreviation === HOU) {
        return <NBAIcons.HOU />
    } else if (teams.data[`${rando}`].abbreviation === IND) {
        return <NBAIcons.IND />
    } else if (teams.data[`${rando}`].abbreviation === LAC) {
        return <NBAIcons.LAC />
    } else if (teams.data[`${rando}`].abbreviation === LAL) {
        return <NBAIcons.LAL />
    } else if (teams.data[`${rando}`].abbreviation === MEM) {
        return <NBAIcons.MEM />
    } else if (teams.data[`${rando}`].abbreviation === MIA) {
        return <NBAIcons.MIA />
    } else if (teams.data[`${rando}`].abbreviation === MIL) {
        return <NBAIcons.MIL />
    } else if (teams.data[`${rando}`].abbreviation === MIN) {
        return <NBAIcons.MIN />
    } else if (teams.data[`${rando}`].abbreviation === NOP) {
        return <NBAIcons.NOP />
    } else if (teams.data[`${rando}`].abbreviation === NYK) {
        return <NBAIcons.NYK />
    } else if (teams.data[`${rando}`].abbreviation === OKC) {
        return <NBAIcons.OKC />
    } else if (teams.data[`${rando}`].abbreviation === ORL) {
        return <NBAIcons.ORL />
    } else if (teams.data[`${rando}`].abbreviation === PHI) {
        return <NBAIcons.PHI />
    } else if (teams.data[`${rando}`].abbreviation === PHX) {
        return <NBAIcons.PHX />
    } else if (teams.data[`${rando}`].abbreviation === POR) {
        return <NBAIcons.POR />
    } else if (teams.data[`${rando}`].abbreviation === SAC) {
        return <NBAIcons.SAC />
    } else if (teams.data[`${rando}`].abbreviation === SAS) {
        return <NBAIcons.SAS />
    } else if (teams.data[`${rando}`].abbreviation === TOR) {
        return <NBAIcons.TOR />
    } else if (teams.data[`${rando}`].abbreviation === UTA) {
        return <NBAIcons.UTA />
    } else if (teams.data[`${rando}`].abbreviation === WAS) {
        return <NBAIcons.WAS />
    }
}

export default CheckTeam